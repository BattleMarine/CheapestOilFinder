# Agent Notes

## 문서 언어 규칙

- `README.md`와 `AGENTS.md`를 수정할 때는 항상 한국어로 작성합니다.

## 현재 언어 규칙

- 앱 구현 코드는 Kotlin을 기본으로 합니다.
- 기존 Java 소스는 제거했고, 신규/수정 코드는 Kotlin으로 작성합니다.

## 현재 지도 마커 규칙

- 현재 위치 화면은 테스트용으로 삼성역을 기준 좌표로 사용하고, 그 주변 주유소를 백엔드에서 받아 지도에 마커로 표시합니다.
- 현재 위치 화면은 진입 시 기기 GPS를 자동으로 읽어 현재 위치 마커를 표시합니다.
- 현재 위치 화면은 진입 즉시 주변 주유소를 자동으로 불러와 리스트와 마커를 함께 표시합니다.
- 현재 위치 화면의 `현재 위치 설정` 버튼은 현재 위치 좌표를 기준으로 반경 5km 주유소를 백엔드에서 요청하고, 주유소 마커를 별도 레이어에 표시합니다.
- 현재 위치 화면의 주유소 조회는 `radiusMeters=5000` 형식을 사용합니다.
- 현재 위치 화면의 `GPS` 버튼은 현재 위치를 강제로 재수신하는 버튼이며, 화면 하단의 리스트와 분리된 고정 위치에 항상 보입니다.
- 현재 위치 화면의 `GPS` 버튼은 눌리는 즉시 캐시된 현재 위치로 먼저 화면 중심을 맞추고, 재수신이 성공하면 다시 현재 위치 마커로 화면을 이동시킵니다.
- 현재 위치 화면의 `GPS` 버튼은 이전 좌표와 10m 미만 차이면 백엔드 재요청을 생략합니다.
- 현재 위치 화면의 `GPS` 버튼은 지도보다 위, 주유소 리스트와 정보 패널보다 아래 레이어에 둡니다.
- 현재 위치 화면 오른쪽 중앙에는 `+`/`-` 줌 버튼을 두고, 이 버튼은 5개/20개/50개/전체를 보여주는 4단계 프리셋만 이동시킵니다.
- 줌 버튼은 GPS 버튼과 같은 크기로 맞추고, 바로 위에 배치하되 둘 사이에는 약간의 간격을 둡니다.
- 손가락 핀치는 기존처럼 자유롭게 확대/축소할 수 있습니다.
- 주유소 마커와 유가 말풍선은 화면 아래쪽에 있는 항목이 더 높은 우선순위를 가져가도록 화면 기준으로 정렬합니다.
- 목적지 화면의 `GPS` 버튼은 기기 위치를 읽어 지도 중심을 갱신하고 현재 위치 마커를 표시합니다.
- 실기기 테스트를 할 때는 `adb reverse tcp:8080 tcp:8080`를 걸고 `secrets/backend_base_url.txt`를 `http://127.0.0.1:8080/`로 둡니다.
- 현재 위치 화면은 주유소를 불러온 뒤 현재 위치와 주유소 좌표가 한 화면에 들어오도록 지도를 자동 맞춤합니다.
- 카카오맵 마커는 `KakaoMapController`의 `LabelLayer` 렌더링으로 관리합니다.
- 현재 위치 마커와 주유소 마커는 분리해서 유지합니다.
- 현재 위치 마커는 파란 점 아이콘으로 표시하고, 주유소 마커보다 위 레이어에 둡니다.
- 주유소 마커는 주유기 모양 아이콘으로 표시하고, 유가 정보는 마커를 가리지 않도록 위쪽 네모 박스로 분리해 표시합니다.
- 현재 위치 라벨 레이어는 `CompetitionType.None`으로 두어 다른 라벨과의 경쟁으로 사라지지 않게 합니다.
- 현재 위치 화면의 GPS 갱신은 위치가 잡히는 즉시 현위치 마커를 먼저 표시합니다.
- 현재 위치와 주유소 마커는 런타임에 생성한 비트맵 스타일로 그립니다.
- 주유소 아이콘은 가격 박스와 분리되어 있으며, 가격 박스가 준비되지 않아도 아이콘은 먼저 표시됩니다.
- 현재 위치 설정 후 나타나는 주유소 마커는 주유기 모양 아이콘과 상단 유가 박스로 표시합니다.
- 주유소 원형 마커는 중심 기준으로 좌표에 맞춰 표시합니다.
- 주유소 마커는 주유기 모양 아이콘과 유가 박스를 분리해서 표시합니다.
- 현재 위치와 주유소 마커는 렌더링 경로를 분리하고, 현재 위치는 주유소보다 위 레이어에 둡니다.
- 같은 Wi-Fi의 로컬 백엔드 HTTP 접속은 디버그 빌드에서만 허용합니다.

이 문서는 이 프로젝트에서 Codex가 작업할 때 지켜야 할 규칙과 현재 합의된 구현 방향을 기록합니다.

## 담당 범위

- 현재 Codex의 주 담당 범위는 Android 프론트엔드입니다.
- 백엔드 서버, 오피넷 수집기, PostgreSQL 스키마 구현은 현재 범위 밖입니다.
- 프론트엔드는 오피넷 등 외부 유가 API를 직접 호출하지 않습니다.
- 주유소, 경로, 추천 결과 데이터는 백엔드 서버에서 받아오는 구조를 전제로 합니다.
- 백엔드가 준비되기 전에는 더미 데이터 또는 placeholder repository로 UI 흐름을 검증합니다.

## 문서 갱신 규칙

기능을 구현하거나 구조를 바꾸면 관련 문서를 함께 갱신합니다.

- 사용자와 제품 진행 상황에 영향을 주는 변경은 `README.md`에 기록합니다.
- 에이전트 작업 규칙, 보안 규칙, 역할 범위, 구현 전제가 바뀌면 `AGENTS.md`에 기록합니다.
- 문서가 깨진 인코딩으로 저장되지 않도록 UTF-8 상태를 확인합니다.
- 사용자가 `AGESTS.md`처럼 오타로 지칭하더라도, 별도 요청이 없는 한 표준 파일명은 `AGENTS.md`로 유지합니다.

## 비밀 정보 규칙

- API 키, 네이티브 앱 키, 서명키, 토큰은 소스 코드에 직접 작성하지 않습니다.
- 로컬 민감 정보는 `secrets/` 폴더에 둡니다.
- 카카오맵 네이티브 앱 키는 `secrets/kakao_native_app_key.txt`에서 읽습니다.
- `secrets/*.txt`, `secrets/*.properties`, `secrets/*.key`, `secrets/*.jks`는 Git에 올리지 않습니다.
- `keystore.properties`, `signing/*.jks`, `signing/*.keystore`도 Git에 올리지 않습니다.
- release 빌드에는 민감한 API 키를 포함하지 않습니다.
- 키 해시나 서명 관련 디버그 로그는 개발 중에만 허용하고, 운영 빌드에서는 제한해야 합니다.
- 사용자가 실제 키를 입력한 파일은 읽거나 출력하지 않는 것을 기본으로 합니다. 필요하면 존재 여부만 확인합니다.

## 프론트엔드 아키텍처 방향

- 지도 SDK 세부 구현은 `KakaoMapController` 안에 모읍니다.
- Activity는 화면 이벤트와 상태 전달에 집중합니다.
- 백엔드 데이터 접근은 repository 계층으로 분리합니다.
- 백엔드 연동은 비동기 네트워크 호출을 전제로 설계합니다.
- 지도, 주유소, 경로, 추천 계산 모델은 SDK 전용 타입에 직접 묶지 않습니다.
- 주유소 마커, 경로 polyline, 추천 결과 오버레이는 지도 SDK 위에 얹는 렌더링 계층으로 확장합니다.
- 외부 내비게이션 앱 연동은 주유소 선택 후 별도 진입점으로 제공합니다.

## 백엔드 전제

백엔드가 담당할 것으로 보는 영역:

- 오피넷 API 호출
- 주유소 데이터 수집 및 정규화
- PostgreSQL 등 DB 저장
- 주변 주유소 검색
- 경로 주변 주유소 검색
- 가격 최신성 관리
- 필요 시 추천 계산 일부 또는 전체 수행

프론트엔드가 담당할 것으로 보는 영역:

- 현재 위치 또는 목적지 입력 UI
- 백엔드 API 호출
- 백엔드 응답을 지도 마커와 리스트로 렌더링
- 추천 결과, 총비용, 계산 근거 표시
- 외부 내비게이션 앱 연동 진입점 제공

## 현재 구현 메모

- 프로젝트는 Android Native Java 기반입니다.
- 카카오맵 SDK v2 의존성이 추가되어 있습니다.
- 지도 화면은 현재 위치 화면과 목적지 화면에 존재합니다.
- 현재 위치 화면은 `map_container`가 화면 전체를 채우고, 뒤로가기/ GPS/ `현 위치로 설정` 버튼이 지도 위에 겹쳐 보이도록 구성합니다.
- 목적지 화면도 같은 방식으로 `map_container`가 화면 전체를 채우고, 뒤로가기/ GPS/ `목적지로 설정` 버튼이 지도 위에 겹쳐 보이도록 구성합니다.
- 앱 시작 시 `ACCESS_FINE_LOCATION` 권한을 확인하고, 없으면 권한 요청을 띄웁니다.
- GPS 버튼으로 읽은 위치는 `KakaoMapController.focusCurrentLocation(...)` 경로를 통해 지도 중심과 현재 위치 마커를 함께 갱신합니다.
- GPS 버튼은 주유소 리스트나 정보 패널이 떠 있어도 항상 보이게 유지합니다.
- 현재 위치 마커 레이어는 주유소 레이어보다 위에 두고, 파란 점 아이콘을 사용합니다.
- 주유소 마커는 주유기 모양 아이콘을 사용하고, 가격은 0원이면 숨깁니다.
- 주유소 가격 박스는 보통휘발유와 고급휘발유가 모두 있으면 `휘발유(고급) 보통가격(고급가격)` 형식으로 한 줄에 묶어 표시합니다.
- 보통휘발유만 있으면 `휘발유 보통가격`, 고급휘발유만 있으면 `고급휘발유 고급가격` 형식으로 표시합니다.
- 디젤은 별도 줄로 표시합니다.
- 현재 위치 레이어는 경쟁하지 않는 설정으로 유지합니다.
- 현재 위치 화면은 GPS 위치 수신 직후 마커를 먼저 그리고, 이후에 주변 주유소를 다시 불러옵니다.
- `KakaoMapConfig`는 `BuildConfig.KAKAO_NATIVE_APP_KEY`만 참조합니다.
- debug 빌드는 `secrets/kakao_native_app_key.txt`에서 키를 읽습니다.
- release 빌드는 카카오맵 키를 빈 값으로 주입합니다.
- x86_64 에뮬레이터에서 카카오맵 네이티브 라이브러리 로딩 문제가 발생할 수 있으며, 이 문제는 현재 개발 환경 이슈로 간주합니다.

## 우선 고려할 문제

- 한국어 문자열 리소스가 깨진 상태라 UI 검증 전에 복구가 필요합니다.
- `StationRepository`가 동기 `List` 반환 구조라 실제 서버 연동 전 비동기 구조로 바꿔야 합니다.
- 지도 시작 좌표가 컨트롤러 내부에 하드코딩되어 있어 외부 상태 주입 구조로 정리해야 합니다.
- `DebugKeyHashLogger`가 항상 호출되면 안전하지 않으므로 debug 조건으로 제한해야 합니다.

## 작업 방식

- 기존 사용자의 변경을 되돌리지 않습니다.
- 수동 파일 편집은 `apply_patch`를 사용합니다.
- 검색은 가능하면 `rg` 또는 `rg --files`를 사용합니다.
- README 또는 AGENTS에 영향을 주는 변경이면 코드 변경과 함께 문서도 갱신합니다.
- 빌드나 테스트가 가능한 변경이면 가능한 한 `gradlew.bat`로 검증합니다.
- 문서만 바꾼 경우에는 빌드 실행을 생략해도 되지만, 생략 사실을 최종 응답에 밝힙니다.

## 커밋 메시지 규칙

- 커밋 전에 이 문서의 커밋 규칙을 다시 확인합니다.
- 커밋 메시지는 기본적으로 `git commit -m "타입: 주된 핵심 변화\n\n- 세세한 변화내용1\n- 세세한 변화내용2"` 형식을 따릅니다.
- 문서 위주의 변경은 `docs:`를 사용합니다.
- 기능 구현 위주의 변경은 `feat:`를 사용합니다.
- 버그 수정 위주의 변경은 `fix:`를 사용합니다.
- 커밋 메시지 본문에는 핵심 변화 1줄과 세부 변경 2개 이상을 bullet로 적습니다.
- 커밋 전에는 이번 변경이 문서, 기능, 버그 수정 중 어디에 속하는지 먼저 분류합니다.
## 프론트엔드 백엔드 연동 메모

- 프론트엔드의 주유소 조회는 Retrofit 기반 `BackendStationRepository`를 통해 수행합니다.
- API 계약, 요청/응답 필드, 호출 예시는 [docs/FRONTEND_BACKEND_API_GUIDE.md](docs/FRONTEND_BACKEND_API_GUIDE.md)에 유지합니다.
- 주변/경로 조회는 `WGS84` 좌표를 사용하며, 기본 유종은 `REGULAR_GASOLINE`, `PREMIUM_GASOLINE`, `DIESEL`입니다.
- Android Emulator의 로컬 백엔드 주소는 `http://10.0.2.2:8080/`를 기본값으로 사용합니다.
- 같은 와이파이의 실기기 테스트에서는 `secrets/backend_base_url.txt`에 적힌 `http://<컴퓨터_LAN_IP>:8080/` 값을 우선 사용합니다.
- 현재 로컬 백엔드에서는 `GET /api/stations/nearby`가 살아 있지만, 요청 형식이 아직 맞지 않아 `400`이 발생합니다.
- `POST /api/stations/route`와 `GET /api/stations/{stationId}`는 현재 `404`를 반환합니다.
- 예전의 `/api/stations/search/nearby` 표기는 사용하지 않습니다.
- `docs/BACKEND_API_ACCESS_CLAIM.md`는 활성 최신본만 유지하고, 이전 실패 기록은 `docs/archive/` 아래로 아카이브합니다.
Retrofit query lists in Kotlin should suppress wildcards when passed through Retrofit interfaces.

## GitHub prep

- Keep `secrets/` entries, signing files, and build outputs out of Git history.
- Treat `secrets/*.example` and `secrets/README.md` as the only safe committed secrets-related files.

## 문서 언어 규칙

- 저장소의 `README.md`와 `AGENTS.md`는 한국어로 작성합니다.
- 새 문서나 기존 문서 수정 시에도 기본적으로 한국어를 사용합니다.

## README 작성 규칙

- `README.md`에는 반드시 프로젝트 소개를 포함합니다.
- `README.md`에는 앱이 어떤 문제를 해결하는지와 핵심 사용 흐름을 포함합니다.
- `README.md`에는 저장소 구조를 포함합니다.
- `README.md`에는 프론트엔드와 백엔드의 역할 분담을 포함합니다.
- `README.md`에는 백엔드 서버가 별도 서비스라는 점과 프론트엔드가 백엔드 API를 호출한다는 점을 포함합니다.
- `README.md`에는 Kakao Map SDK 사용 사실과 지도 마커 역할을 포함합니다.
- `README.md`에는 빌드 방법과 현재 상태 메모를 포함합니다.

## 비밀정보 문서 규칙

- `README.md`에는 비밀정보의 실제 값은 적지 않습니다.
- `README.md`에는 비밀정보의 경로, 파일명, 기대 형식만 적습니다.
- `README.md`에는 예시 값만 표기하고 실제 키 문자열은 절대 적지 않습니다.
- 카카오 네이티브 앱 키는 `secrets/kakao_native_app_key.txt`에서 읽는다고 명시합니다.
- `secrets/` 아래의 실제 비밀 파일들은 `.gitignore`로 무조건 제외되어야 합니다.
- 비밀정보 관련 예제 파일과 안내 문서만 Git에 남깁니다.

## 브랜드 로고 규칙

- 주유소 마커는 `logo/`와 `drawable-nodpi/`에 있는 브랜드 로고의 짧은 버전을 사용합니다.
- 주유소 리스트는 브랜드의 전체 로고를 좌측에 표시합니다.
- 로고는 해상도가 달라도 같은 기준 박스 안에서 보이도록 크기를 통일합니다.
- 로고는 투명 PNG를 그대로 사용하고, 별도 배경 박스는 두지 않습니다.

## 최근 렌더링 메모

- 주유소 마커와 유가 말풍선은 서로 겹쳐도 뒤쪽 항목을 강제로 숨기지 않고 그대로 유지합니다.
- 화면 이동 시에는 기존 라벨을 재사용하고, 필요한 항목만 추가/삭제해서 깜박임을 줄입니다.
- 화면 기준 정렬은 유지하되, 라벨 경쟁은 끄고 `CompetitionType.None`으로 둡니다.

## 저장소 소개 문서 규칙

- README의 저장소 구조는 실제 디렉터리와 파일 배치를 반영합니다.
- README에는 백엔드 API 가이드 문서 위치를 링크로 남깁니다.
- README에는 로컬 개발에서 사용한 백엔드 주소나 검증 상태를 필요 시 기록합니다.
- README에는 사용자가 GitHub에서 처음 봤을 때 필요한 정보를 우선 배치합니다.
## 현재 위치 화면 추가 규칙

- 현재 위치 화면의 주유소 마커는 줌 레벨에 따라 유가 박스 표시 수준이 달라집니다.
- 확대가 충분하면 보통휘발유, 고급휘발유, 디젤을 모두 표시합니다.
- 어느 정도 줌아웃하면 디젤을 숨기고 휘발유 계열만 표시합니다.
- 더 줌아웃하면 유가 텍스트 박스를 모두 숨기고 주유소 아이콘만 표시합니다.
- `현재 위치로 설정` 버튼을 누르면 반경 5km 주유소가 하단 반쯤 올라오는 리스트로 표시됩니다.
- 주유소 리스트는 보통휘발유 가격이 낮은 순으로 정렬하며, 거리와 브랜드, 주유소 이름, 유가를 함께 보여줍니다.
- 리스트 상단 손잡이를 드래그하거나 뒤로가기를 누르면 현재 상태에서 한 단계 아래 상태로 접힙니다.
## 최근 반영 규칙

- 현재 위치 화면의 주유소 리스트는 한 번 로드되면 하단의 최소화 상태를 유지하고, 헤더를 누르면 다시 펼칠 수 있어야 합니다.
- 주유소 가격 박스는 줌 11~13에서 단계적으로 밝아지고, 줌 14~15에서 완전히 보이며, 줌 16~17에서는 더 큰 버전으로 보여야 합니다.
- 주유소 가격 박스는 줌아웃 단계에 따라 서서히 투명해졌다가, 줌 10 이하에서는 사라져야 합니다.
- 주유소 가격 박스는 흰색 배경과 검은 글자로 표시합니다.

## 최신 줌 규칙

- 줌 10에서는 주유소 아이콘만 보입니다.
- 줌 11과 12에서는 주유소 가격 박스가 단계적으로 더 밝아집니다.
- 줌 13부터 15까지는 주유소 가격 박스가 완전히 노출됩니다.
- 줌 16 이상에서는 더 큰 가격 박스 버전으로 자세히 표시합니다.
- 가격 박스는 겹쳐도 어두워지지 않도록 흰색 배경을 유지하고, 투명도는 글자 중심으로만 조절합니다.

## UI 보조 규칙

- 주유소 리스트나 정보 패널이 보이는 동안에는 `현재 위치로 설정` 버튼을 숨기고, 패널이 완전히 숨겨졌을 때만 다시 나타나야 합니다.
- 주유소 리스트의 드래그 제스처는 상단의 작은 가로 손잡이에서만 받아야 합니다.
- 실제 드래그 터치 영역은 손잡이보다 더 두껍고 가로로 길게 잡아, 가운데뿐 아니라 가장자리 쪽에서도 드래그가 잡히게 합니다.
- 주유소 리스트의 배경 스크림은 반개 또는 최소화 상태에서 터치를 가져가면 안 됩니다.
- 주유소 리스트 본문은 스크롤만 담당하고, 시트 확장/축소 제스처는 손잡이 터치 영역만 담당해야 합니다.

## 주유소 로고 및 줌 규칙

- 주유소 마커 로고는 조금 더 작은 크기로 표시합니다.
- 유가 말풍선은 마커의 우상단으로 치우치게 그리고, 꼬리를 통해 주유소 마커를 가리키게 합니다.
- 유가 말풍선은 완전 불투명하지 않게 약간의 투명도를 적용합니다.
- 충분히 줌아웃하면 일반 주유소 마커와 유가 말풍선은 사라지고, 최저가 상위 5개 주유소만 남깁니다.
- 유가 말풍선의 배경과 테두리도 투명도 값과 함께 줄어들어야 합니다.

## 주유소 마커 및 유가 말풍선 세부 규칙

- 주유소 로고는 이전보다 조금 더 작은 크기로 유지합니다.
- 유가 말풍선은 우상향으로 배치하되, 꼬리는 중앙 하단 기준으로 주유소 마커를 가리키게 합니다.
- 유가 말풍선의 배경 투명도는 사용하지 않고, 말풍선은 불투명한 흰색 배경으로 표시합니다.
- 지도를 많이 줌아웃하면 일반 주유소 마커와 유가 말풍선은 더 빨리 사라지고, 최저가 상위 5개 주유소만 남깁니다.
- 최저가 상위 5개 주유소는 유가 말풍선을 유지합니다.
- 유가 말풍선의 글자 투명도는 줌아웃에 따라 로그형으로 빠르게 감소합니다.

## 주유소 마커 및 유가 말풍선 추가 규칙

- 충분히 줌아웃하면 top 5 주유소만 보이도록 합니다.
- 그보다 조금 더 줌인하면 top 20 주유소를 보이도록 합니다.
- 더 줌인하면 전체 주유소를 보이도록 합니다.
- top 5와 top 20 구간의 유가 말풍선은 완전 불투명보다 약간 약하게 보여 줍니다.
- top 5와 top 20 구간에서는 렌더링 대상을 줄여 지도가 덜 가려지도록 합니다.

## 주유소 마커 및 유가 말풍선 추가 규칙

- 충분히 줌아웃하면 top 5 주유소만 보이도록 합니다.
- 그보다 조금 더 줌인하면 top 20 주유소가 보이도록 합니다.
- 더 줌인하면 전체 주유소가 더 빨리 보이도록 합니다.
- top 5와 top 20 구간의 유가 말풍선은 완전 불투명보다 약간 약하게 보여 줍니다.
- top 5와 top 20 구간에서는 렌더링 대상을 줄여 지도가 덜 가려지도록 합니다.
- 유가 말풍선의 전체 정보는 늦게 풀리도록 해서 13~14 구간은 간소화된 상태를 유지합니다.

## 유가 말풍선 위치 조정

- 유가 말풍선은 이전보다 더 작게 표시합니다.
- 유가 말풍선은 조금 더 위로 올라가도록 조정합니다.
- 유가 말풍선의 꼬리는 가운데 하단 기준을 유지합니다.
## 주유소 표시 구간 규칙

- 지도가 충분히 줌아웃되면 top 5 주유소만 보이도록 합니다.
- 그보다 조금 더 줌인하면 top 20 주유소가 보이도록 합니다.
- top 20과 전체 사이 구간에서는 top 50 주유소가 보이도록 합니다.
- 더 줌인하면 전체 주유소가 보이도록 합니다.
- top 5, top 20, top 50 구간의 유가 말풍선은 완전 불투명보다 약간 약하게 보여 줍니다.
- top 5, top 20, top 50 구간에서는 렌더링 대상을 줄여 지도가 덜 가려지도록 합니다.

## 현재 위치 원 표시 규칙

- 현재 위치 화면에는 현재 위치를 기준으로 반경 5km 원을 선만 그려 표시합니다.
- 이 원은 주유소 마커보다 아래에 두고, 지도 레이어 바로 위에 배치합니다.
- 원은 채우지 않고 윤곽선만 사용합니다.

## 주유소 정보 패널 규칙

- 주유소 마커를 누르면 해당 주유소를 화면 가운데로 옮기고, 화면 상단에서 정보 패널을 내려옵니다.
- 주유소 리스트 항목을 눌러도 동일한 정보 패널을 내려옵니다.
- 정보 패널이 떠 있는 동안에는 화면 전체 스크림이 다른 버튼 입력을 막지만, 배경은 어둡게 만들지 않습니다.
- 정보 패널은 상단 스와이프로 닫을 수 있고, 패널 밖을 누르면 자동으로 닫힙니다.

## 주유소 정보 패널 방향 변경

- 주유소 정보 패널은 이제 화면 아래에서 위로 올라오는 방식으로 표시합니다.
- 정보 패널은 화면의 절반까지만 올라오고, 지도는 상단 절반을 유지합니다.
- 리스트와 정보 패널은 위로 스와이프하면 전체 화면으로 확장합니다.
- 정보 패널은 아래로 절반 정도만 내려도 닫히도록 합니다.
## 추가 UI 동작 규칙

- 주유소 리스트는 기본적으로 하단 반개 상태로 열리고, 위로 스와이프하면 전체 화면으로 확장합니다.
- 리스트가 열려 있는 동안에는 `현재 위치로 설정` 버튼을 숨기고, 리스트를 닫아야 다시 보이게 합니다.
- 리스트가 절반 상태일 때 위로 스와이프하면 전체 화면으로 확장되고, 아래로 스와이프하면 최소화 상태로 접힙니다.
- 리스트가 전체 화면일 때 아래로 한 번 스와이프하면 절반 상태로 돌아갑니다.
- 리스트가 전체 화면일 때 위로 스와이프해도 반응하지 않습니다.
- 뒤로가기는 전체 화면 -> 절반 -> 최소화 순서로만 동작하고, 최소화 상태에서는 닫히지 않습니다.
- 최소화 상태에서 뒤로가기를 한 번 누르면 메인화면 이동 안내 토스트를 띄우고, 짧은 시간 안에 한 번 더 누르면 메인화면으로 나갑니다.
- 아래로 스와이프는 뒤로가기와 동일한 의미로 취급합니다.
- 리스트 본문에서는 패널 상태 전환 제스처를 받지 않고, 항목 스크롤을 유지합니다.
- 절반 상태에서 아래로 스와이프하면 실제로 최소화 위치까지 내려갑니다.
- 리스트 본문 스크롤은 유지하고, 전체화면에서는 위로 스와이프해도 상태가 변하지 않게 합니다.
- 주유소 정보 패널도 하단에서 올라오며, 위로 스와이프하면 전체 화면으로 확장합니다.
- 주유소 정보 패널은 아래로 약간만 내려도 닫히도록 하며, 패널이 떠 있는 동안에는 패널이 우선 입력을 가져갑니다.
- 주유소 리스트에서 뒤로가기는 전체 화면 -> 절반 -> 최소화 순서로 단계적으로 동작하며, 최소화 상태에서는 더 이상 닫히지 않습니다.
- 주유소 정보 패널은 닫힘 민감도를 높여, 아래로 절반쯤만 내려도 쉽게 닫히게 합니다.
